x = float(input("Enter first decimal number: "))
y = float(input("Enter second decimal number: "))

product = x * y

print("The product is:", round(product, 2))

#Explanation:
float() converts input to decimal. round(product, 2) keeps 2 decimal places
